'use client';

export default function MainPage() {
  return (
    <main className="relative flex min-h-dvh items-center justify-center bg-primary-500">
      <span className="text-white">웨딧</span>
    </main>
  );
}